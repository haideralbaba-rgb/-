import { useEffect, useState } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { FiArrowRight, FiCheck, FiMapPin, FiMinus, FiPlus, FiTrash2, FiX, FiNavigation } from "react-icons/fi";
import { useOrder } from "../context/OrderContext";
import { useAuth } from "../context/AuthContext";
import { useGeolocation, type GeoResult } from "../hooks/useGeolocation";
import { restaurantConfig } from "../data/restaurantConfig";
import { createOrder } from "../lib/orderService";

type Step = "cart" | "location" | "confirm" | "processing" | "success" | "error";
type Fulfillment = "delivery" | "pickup";
const DELIVERY_FEE = 3000;
const MINIMUM_DELIVERY = 10000;
const titles: Record<Step,string> = { cart:"السلة", location:"موقع التوصيل", confirm:"تأكيد الطلب", processing:"هسه نرسل...", success:"تم الطلب", error:"صار خطأ" };

export default function OrderDrawer() {
  const { items,isCartOpen,closeCart,updateQuantity,removeItem,totalPrice,totalItems,clearCart } = useOrder();
  const { user,profile,addresses,setShowLogin,setLoginRedirectTo,saveAddress } = useAuth();
  const geo = useGeolocation();
  const [step,setStep] = useState<Step>("cart");
  const [fulfillment,setFulfillment] = useState<Fulfillment>(restaurantConfig.deliveryAvailable ? "delivery" : "pickup");
  const [selectedAddress,setSelectedAddress] = useState<GeoResult|null>(null);
  const [manualAddress,setManualAddress] = useState("");
  const [notes,setNotes] = useState("");
  const [customerName,setCustomerName] = useState("");
  const [orderNumber,setOrderNumber] = useState("");
  const [orderError,setOrderError] = useState("");
  const [submitting,setSubmitting] = useState(false);

  useEffect(()=>{ if(!isCartOpen){ const t=window.setTimeout(()=>{setStep("cart");setSelectedAddress(null);setManualAddress("");setNotes("");setOrderNumber("");setOrderError("");setSubmitting(false);geo.reset();},250); return()=>window.clearTimeout(t);}},[isCartOpen]);
  useEffect(()=>{if(profile?.name)setCustomerName(profile.name)},[profile]);
  useEffect(()=>{if(user&&isCartOpen&&items.length&&step==="cart")setStep("location")},[user,isCartOpen,items.length,step]);

  const back=()=>{if(submitting||step==="processing"||step==="success")return;setOrderError("");if(step==="location")setStep("cart");else if(step==="confirm")setStep("location");else if(step==="error")setStep("confirm")};
  const proceed=()=>{if(!items.length)return;if(!user){setLoginRedirectTo("checkout");setShowLogin(true);return;}setOrderError("");setStep("location")};
  const useGps=async()=>{setOrderError("");const r=await geo.requestLocation();if(r)setSelectedAddress(r)};
  const confirmLocation=()=>{if(fulfillment==="delivery"&&!selectedAddress&&!manualAddress.trim())return setOrderError("حدد موقع التوصيل أولاً");if(fulfillment==="delivery"&&totalPrice<MINIMUM_DELIVERY)return setOrderError(`الحد الأدنى للتوصيل ${MINIMUM_DELIVERY.toLocaleString("en-US")} د.ع`);setOrderError("");setStep("confirm")};
  const deliveryFee=fulfillment==="delivery"?DELIVERY_FEE:0;
  const grandTotal=totalPrice+deliveryFee;

  const submit=async()=>{if(submitting||!user||!items.length)return;setSubmitting(true);setOrderError("");setStep("processing");const a=selectedAddress;const result=await createOrder({customerId:profile?.id||user.id,items,subtotal:totalPrice,deliveryFee,total:grandTotal,fulfillment,latitude:fulfillment==="delivery"?a?.latitude??null:null,longitude:fulfillment==="delivery"?a?.longitude??null:null,formattedAddress:fulfillment==="delivery"?a?.formattedAddress||manualAddress.trim()||null:null,phone:profile?.phone||"",customerName:customerName||profile?.name||null,notes:notes.trim()||null});if(result.success&&result.orderNumber){setOrderNumber(result.orderNumber);if(fulfillment==="delivery"&&a){try{await saveAddress({label:"موقعي",latitude:a.latitude,longitude:a.longitude,formatted_address:a.formattedAddress||"",city:a.city||null,district:a.district||null,street:a.street||null,building:null,delivery_notes:notes.trim()||null,is_default:addresses.length===0})}catch{}}clearCart();setSubmitting(false);setStep("success");return}setSubmitting(false);setOrderError(result.error||"صار خطأ أثناء إنشاء الطلب");setStep("error")};

  return <AnimatePresence>{isCartOpen&&<><motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={closeCart} className="fixed inset-0 z-[90] bg-black/75 backdrop-blur-sm"/><motion.div initial={{y:"100%",opacity:0}} animate={{y:0,opacity:1}} exit={{y:"100%",opacity:0}} transition={{type:"spring",damping:30,stiffness:280}} role="dialog" aria-modal="true" className="fixed inset-x-0 bottom-0 z-[95] flex max-h-[94svh] flex-col rounded-t-3xl border-t border-white/[0.08] bg-bg-secondary shadow-2xl shadow-black lg:inset-y-0 lg:right-0 lg:left-auto lg:max-h-none lg:w-full lg:max-w-md lg:rounded-none lg:border-t-0 lg:border-l">
    <header className="flex shrink-0 items-center gap-3 border-b border-white/[0.08] px-5 py-4">
      {step!=="cart"&&step!=="processing"&&step!=="success"&&<button onClick={back} aria-label="رجوع" className="grid h-9 w-9 place-items-center rounded-full border border-white/15 text-ivory-dim hover:border-gold hover:text-gold"><FiArrowRight size={17}/></button>}
      <h3 className="font-display text-xl text-ivory">{titles[step]}</h3>
      <button onClick={closeCart} aria-label="إغلاق" className="mr-auto grid h-9 w-9 place-items-center rounded-full border border-white/15 text-ivory-dim hover:bg-white/10"><FiX size={16}/></button>
    </header>
    <div className="flex-1 overflow-y-auto">
      {step==="cart"&&<div className="px-5 py-4">{!items.length?<div className="py-16 text-center"><p className="font-display text-xl text-ivory">السلة فاضية</p><p className="mt-2 text-sm text-ivory-mute">ارجع للقائمة واختار اللي يعجبك.</p></div>:<div className="space-y-3">{items.map(i=><div key={i.id} className="flex items-center gap-3 border-b border-white/[.06] pb-3">{i.image&&<img src={i.image} alt="" className="h-14 w-14 shrink-0 rounded-xl object-cover"/>}<div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-ivory">{i.name}</p>{i.variant&&<p className="text-xs text-gold">{i.variant}</p>}<p className="numeric text-xs text-ivory-mute">{i.price.toLocaleString("en-US")} د.ع</p></div><div className="flex items-center gap-1"><button onClick={()=>updateQuantity(i.id,i.quantity-1)} className="grid h-8 w-8 place-items-center rounded-full border border-white/15"><FiMinus size={11}/></button><span className="numeric w-5 text-center text-sm">{i.quantity}</span><button onClick={()=>updateQuantity(i.id,i.quantity+1)} className="grid h-8 w-8 place-items-center rounded-full border border-white/15"><FiPlus size={11}/></button></div><p className="numeric w-16 text-left text-sm font-bold text-gold">{(i.price*i.quantity).toLocaleString("en-US")}</p><button onClick={()=>removeItem(i.id)} aria-label="حذف" className="grid h-8 w-8 place-items-center text-ivory-mute hover:text-red-accent"><FiTrash2 size={14}/></button></div>)}</div>}</div>}
      {step==="location"&&<div className="px-5 py-5"><p className="mb-3 text-sm font-bold text-ivory">طريقة الاستلام</p><div className="mb-6 grid grid-cols-2 gap-3">{restaurantConfig.deliveryAvailable&&<button onClick={()=>setFulfillment("delivery")} className={`rounded-xl border px-4 py-3 text-sm ${fulfillment==="delivery"?"border-gold bg-gold/10 text-gold":"border-white/15 text-ivory-dim"}`}>توصيل</button>}{restaurantConfig.pickupAvailable&&<button onClick={()=>setFulfillment("pickup")} className={`rounded-xl border px-4 py-3 text-sm ${fulfillment==="pickup"?"border-gold bg-gold/10 text-gold":"border-white/15 text-ivory-dim"}`}>استلام من الفرع</button>}</div>{fulfillment==="delivery"?<><p className="mb-2 text-sm font-bold text-ivory">عنوان التوصيل</p>{addresses.length>0&&!selectedAddress&&<div className="mb-4 space-y-2">{addresses.map(a=><button key={a.id} onClick={()=>setSelectedAddress({latitude:a.latitude,longitude:a.longitude,formattedAddress:a.formatted_address,city:a.city||"",district:a.district||"",street:a.street||""})} className="flex w-full items-center gap-3 rounded-xl border border-white/10 bg-surface px-4 py-3 text-right"><FiMapPin className="text-gold"/><div className="min-w-0 flex-1"><p className="truncate text-sm text-ivory">{a.label}</p><p className="truncate text-xs text-ivory-mute">{a.formatted_address}</p></div></button>)}</div>}{!selectedAddress&&<div className="rounded-xl border border-white/10 bg-surface p-5 text-center"><FiNavigation className="mx-auto text-2xl text-gold"/><p className="mt-3 font-display text-lg text-ivory">حدد موقعك</p><button onClick={useGps} disabled={geo.status==="requesting"||geo.status==="geocoding"} className="mt-4 w-full rounded-full bg-red py-3 text-sm font-bold text-ivory disabled:opacity-50">{geo.status==="requesting"?"هسه نحدد موقعك...":geo.status==="geocoding"?"هسه نجهز عنوانك...":"حدد موقعي"}</button>{(geo.status==="denied"||geo.status==="error")&&<><p className="mt-3 text-sm text-red-accent">{geo.errorMessage}</p><input value={manualAddress} onChange={e=>setManualAddress(e.target.value)} placeholder="المنطقة، الشارع، أقرب نقطة دالة" className="mt-4 w-full rounded-xl border border-white/15 bg-bg px-4 py-3 text-sm text-ivory"/></>}</div>}{selectedAddress&&<div className="rounded-xl border border-gold-muted/30 bg-surface p-4"><div className="flex gap-3"><FiMapPin className="mt-1 text-gold"/><div><p className="text-sm font-bold text-ivory">موقع التوصيل</p><p className="mt-1 text-sm leading-6 text-ivory-dim">{selectedAddress.formattedAddress}</p></div></div><button onClick={()=>{setSelectedAddress(null);geo.reset()}} className="mt-3 text-xs text-gold">تغيير الموقع</button></div>}<div className="mt-4 rounded-xl border border-white/10 bg-surface p-4"><div className="flex justify-between text-sm"><span className="text-ivory-dim">قيمة الطلب</span><span className="numeric">{totalPrice.toLocaleString("en-US")} د.ع</span></div><div className="mt-2 flex justify-between text-sm"><span className="text-ivory-dim">التوصيل</span><span className="numeric">{DELIVERY_FEE.toLocaleString("en-US")} د.ع</span></div></div>{orderError&&<p className="mt-4 text-sm text-red-accent">{orderError}</p>}</>:<div className="rounded-xl border border-white/10 bg-surface p-5 text-center"><FiMapPin className="mx-auto text-2xl text-gold"/><p className="mt-3 font-display text-lg text-ivory">استلام من الفرع</p><p className="mt-1 text-sm text-ivory-mute">طلبك يصير جاهز للاستلام من المطعم.</p></div>}</div>}
      {step==="confirm"&&<div className="px-5 py-5"><div className="mb-5 rounded-xl border border-white/[.08] bg-surface p-4"><p className="mb-3 text-sm font-bold text-ivory">ملخص الطلب</p>{items.map(i=><div key={i.id} className="flex justify-between py-1.5 text-sm"><span className="text-ivory-dim">{i.name} × {i.quantity}</span><span className="numeric">{(i.price*i.quantity).toLocaleString("en-US")}</span></div>)}<div className="mt-3 border-t border-white/[.08] pt-3"><div className="flex justify-between font-bold"><span>الإجمالي</span><span className="numeric text-gold">{grandTotal.toLocaleString("en-US")} د.ع</span></div></div></div>{!profile?.name&&<input value={customerName} onChange={e=>setCustomerName(e.target.value)} placeholder="اسمك" className="mb-4 w-full rounded-xl border border-white/15 bg-surface px-4 py-3 text-ivory"/>}<textarea value={notes} onChange={e=>setNotes(e.target.value)} rows={3} placeholder="ملاحظات للسائق (اختياري)" className="mb-4 w-full resize-none rounded-xl border border-white/15 bg-surface px-4 py-3 text-ivory"/>{fulfillment==="delivery"&&<div className="rounded-xl border border-gold-muted/20 bg-gold/5 p-4 text-sm text-gold">التوصيل إلى: {selectedAddress?.formattedAddress||manualAddress||"الموقع المحدد"}</div>}</div>}
      {step==="processing"&&<div className="flex flex-col items-center justify-center px-5 py-20"><div className="h-10 w-10 animate-spin rounded-full border-4 border-gold-muted/30 border-t-gold"/><p className="mt-5 font-display text-lg text-ivory">هسه نجهز طلبك...</p><p className="mt-2 text-sm text-ivory-mute">لحظات، لا تسكر الصفحة.</p></div>}
      {step==="success"&&<div className="flex flex-col items-center px-5 py-12 text-center"><motion.div initial={{scale:0}} animate={{scale:1}} className="grid h-20 w-20 place-items-center rounded-full bg-gold text-bg"><FiCheck size={34}/></motion.div><h4 className="mt-5 font-display text-2xl text-ivory">تم تأكيد طلبك</h4><p className="numeric mt-5 rounded-xl border border-gold-muted/30 bg-surface px-6 py-3 font-display text-2xl text-gold">{orderNumber}</p><p className="mt-5 text-sm leading-7 text-ivory-dim">طلبك وصل للمطعم وراح نبدأ بتجهيزه.</p></div>}
      {step==="error"&&<div className="flex flex-col items-center px-5 py-16 text-center"><div className="grid h-16 w-16 place-items-center rounded-full bg-red/20 text-red-accent"><FiX size={30}/></div><h4 className="mt-5 font-display text-2xl text-ivory">ما اكتمل الطلب</h4><p className="mt-3 max-w-sm text-sm leading-7 text-red-accent">{orderError||"حدث خطأ غير متوقع"}</p><button onClick={()=>{setOrderError("");setStep("confirm")}} className="mt-7 rounded-full bg-gold px-8 py-3 font-bold text-bg">رجوع للمراجعة</button></div>}
    </div>
    {step==="cart"&&items.length>0&&<div className="shrink-0 border-t border-white/[.08] bg-bg-secondary p-5"><div className="mb-3 flex items-center justify-between"><span className="text-sm text-ivory-dim">{totalItems} عناصر</span><span className="numeric text-lg font-bold text-gold">{totalPrice.toLocaleString("en-US")} د.ع</span></div><button onClick={proceed} className="w-full rounded-full bg-red py-4 font-display text-lg text-ivory">متابعة الطلب</button></div>}
    {step==="location"&&<div className="shrink-0 border-t border-white/[.08] bg-bg-secondary p-5"><button onClick={fulfillment==="pickup"?()=>setStep("confirm"):confirmLocation} disabled={fulfillment==="delivery"&&!selectedAddress&&!manualAddress.trim()} className="w-full rounded-full bg-red py-4 font-display text-lg text-ivory disabled:opacity-50">متابعة لتأكيد الطلب</button></div>}
    {step==="confirm"&&<div className="shrink-0 border-t border-white/[.08] bg-bg-secondary p-5"><button onClick={submit} disabled={submitting} className="w-full rounded-full bg-red py-4 font-display text-lg text-ivory disabled:opacity-50">{submitting?"هسه نرسل...":"تأكيد وإرسال الطلب"}</button></div>}
    {step==="success"&&<div className="shrink-0 border-t border-white/[.08] bg-bg-secondary p-5"><button onClick={closeCart} className="w-full rounded-full bg-gold py-4 font-display font-bold text-bg">تمام</button></div>}
  </motion.div></>}</AnimatePresence>;
}
